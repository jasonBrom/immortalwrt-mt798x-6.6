# Optional patches

## 500-tx_power.patch

This patch modifies wireless-regdb and raises regulatory power limits for many countries.

It is not required for BE14 detection or MTK driver support.

Default workflow input `include_regdb_patch` must remain `false`.

Enable it only for lab testing and only where legally allowed.
